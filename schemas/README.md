# Schemas (placeholder — milestone A)

Machine-readable schemas for the durable rows live implicitly in
`app/db.py` (SCHEMA) and the response models in `app/models.py`.
Future milestones may export versioned JSON schemas here for the
report-snapshot and evidence-bundle formats.
